export default function Button({ title }) {
	return (
		<button className="mt-[2rem] px-[1.75rem] py-[.5rem] bg-[#2500FF] text-[#fff] rounded-lg text-[.875rem] font-semibold cursor-pointer ">
			{title}
		</button>
	);
}
